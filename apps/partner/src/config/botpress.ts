// Botpress Webchat — shareable full-page chat.
// This is the "Shareable" link from Botpress Cloud > Webchat > Share.
// It renders identically inside an <iframe> (web) and a WebView (native),
// which is why we use the shareable URL rather than the inject.js snippet
// (the latter is a floating bubble that needs full DOM/window access).
export const BOTPRESS_WEBCHAT_URL =
  'https://cdn.botpress.cloud/webchat/v5.0/shareable.html?configUrl=https://files.bpcontent.cloud/2026/07/28/05/20260728050246-VJP1IEU1.json';
